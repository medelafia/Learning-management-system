spring:
  datasource:
    url: jdbc:mysql://${EXAM_DB_HOST}:${EXAM_DB_PORT}/lms-exam?createDatabaseIfNotExist=true
    username: ${EXAM_DB_USERNAME}
    password: ${EXAM_DB_PASSWORD}
eureka:
  instance:
    hostname: ${EUREKA_HOST}
  client:
    service-url:
      defaultZone: http://${EUREKA_HOST}:8761/eureka
