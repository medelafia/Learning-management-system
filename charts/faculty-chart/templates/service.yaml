apiVersion: v1
kind: Service
metadata:
  name: {{ include "faculty-chart.fullname" . }}
  labels:
    {{- include "faculty-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "faculty-chart.selectorLabels" . | nindent 4 }}
