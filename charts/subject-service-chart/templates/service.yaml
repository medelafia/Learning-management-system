apiVersion: v1
kind: Service
metadata:
  name: subject-service
  labels:
    {{- include "subject-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "subject-chart.selectorLabels" . | nindent 4 }}
